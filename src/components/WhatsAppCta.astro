---
import { buildWhatsAppUrl, getWhatsAppMessage } from '../data/whatsapp';

type Variant = 'hero' | 'navbar' | 'contact';

interface Props {
  variant: Variant;
  label?: string;
}

const { variant, label } = Astro.props;
const whatsappUrl = buildWhatsAppUrl(getWhatsAppMessage(Astro.url.pathname));

const defaultLabels: Record<Variant, string> = {
  hero: 'Quero agendar meu exame',
  navbar: 'Quero agendar uma consulta',
  contact: 'Entre em contato',
};

const text = label ?? defaultLabels[variant];
const ariaLabel = `${text} (abre no WhatsApp)`;
---

{
  variant === 'hero' && (
    <a
      href={whatsappUrl}
      class="hero__cta motion-btn"
      target="_blank"
      rel="noopener noreferrer"
      aria-label={ariaLabel}
    >
      <img
        class="hero__cta-icon"
        src="/assets/shared/cta-arrow.svg"
        alt=""
        width="20"
        height="20"
        decoding="async"
        fetchpriority="low"
        aria-hidden="true"
      />
      {text}
    </a>
  )
}

{
  variant === 'navbar' && (
    <a
      href={whatsappUrl}
      class="btn-cta motion-btn"
      target="_blank"
      rel="noopener noreferrer"
      aria-label={ariaLabel}
    >
      {text}
      <img
        class="btn-cta__icon"
        src="/assets/shared/cta-arrow.svg"
        alt=""
        width="20"
        height="20"
        decoding="async"
        fetchpriority="low"
        aria-hidden="true"
      />
    </a>
  )
}

{
  variant === 'contact' && (
    <a
      href={whatsappUrl}
      class="service-card__contact-btn"
      target="_blank"
      rel="noopener noreferrer"
      aria-label={ariaLabel}
    >
      <span class="service-card__contact-text">{text}</span>
    </a>
  )
}
